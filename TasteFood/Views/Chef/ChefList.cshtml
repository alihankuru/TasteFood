@model List<TasteFood.Entites.Chef>
@{
    ViewBag.Title = "ChefList";
    Layout = "~/Views/AdminLayout/Index.cshtml";
}

<h2>Şef Listesi</h2>

<div class="container-fluid" id="container-wrapper">
    <div class="d-sm-flex align-items-center justify-content-between mb-4">
        <ol class="breadcrumb">
            <li class="breadcrumb-item"><a href="./">Ana Sayfa</a></li>
            <li class="breadcrumb-item">Şefler</li>
            <li class="breadcrumb-item active" aria-current="page">Şef İşlemeleri</li>
        </ol>
    </div>

    <div class="row">
        <div class="col-lg-12 mb-4">
            <!-- Simple Tables -->
            <div class="card">
          
                <div class="table-responsive">
                    <table class="table align-items-center table-flush">
                        <thead class="thead-light">
                            <tr>
                                <th>Chef ID</th>
                                <th>İsim Soyisim</th>
                                <th>Title</th>
                                <th>ImageUrl</th>

                                <th>Açıklama</th>
                                <th>Sil</th>

                                <th>Güncelle</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var item in Model)
                            {

                                <tr>
                                    <td>@item.ChefId</td>

                                    <td>@item.NameSurname</td>
                                    <td>@item.Title</td>
                                    <td><img src="@item.ImageUrl" style="height:50px;width:50px;" class="rounded mx-auto d-block" alt="@item.ImageUrl"></td>
                                    <td>@item.Description</td>


                                    <td><a href="/Chef/DeleteChef/@item.ChefId" class="btn btn-sm btn-primary">Sil</a></td>
                                    <td><a href="/Chef/UpdateChef/@item.ChefId" class="btn btn-sm btn-primary">Güncelle</a></td>
                                </tr>
                            }
                        </tbody>
                    </table>
                    <td><a href="/Chef/CreateChef/" class="btn btn-sm btn-primary">Yeni Şef Girişi</a></td>
                </div>
                <div class="card-footer"></div>
            </div>
        </div>
    </div>
    <!--Row-->


</div>